{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://channel-maker.local/engine/identity/identity-candidate-review.schema.json",
  "title": "Channel Identity Candidate Human Review v1",
  "type": "object",
  "additionalProperties": false,
  "required": ["schema_version", "artifact_type", "review_id", "candidate_id", "target_sha256", "decision", "reviewer", "reason", "created_at"],
  "properties": {
    "schema_version": {"const": "1.0.0"},
    "artifact_type": {"const": "identity_candidate_review"},
    "review_id": {"type": "string", "pattern": "^identity-candidate-review:[a-f0-9]{16}$"},
    "candidate_id": {"$ref": "common.schema.json#/$defs/candidateId"},
    "target_sha256": {"type": "string", "pattern": "^[a-f0-9]{64}$"},
    "decision": {"enum": ["approved", "rejected", "borderline"]},
    "reviewer": {"type": "string", "minLength": 1},
    "reason": {"type": "string", "minLength": 1},
    "created_at": {"type": "string", "format": "date-time"}
  }
}
